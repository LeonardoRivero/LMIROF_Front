export interface ItemDTO {
  id: number;
  title: string;
  price: number;
  url: string;
  quantity: number;
}

export interface IdentificationTypeDTO {
  id: number;
  description: string;
  abbreviation: string;
}

export interface GenderDTO {
  id: number;
  name_gender: string;
}
